# clouditera-sdk

clouditera-sdk是clouditera内部封装的go SDK仓库。

## 使用方法

```bash
export GOPRIVATE=source.gitlab.clouditera.com
export GOINSECURE=source.gitlab.clouditera.com
go get source.gitlab.clouditera.com/clouditera/go-common-sdk/v1
```
